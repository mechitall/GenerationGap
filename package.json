{
  "name": "family-connect-app",
  "version": "1.0.0",
  "description": "A family communication app that helps parents and teenagers understand each other through shared journals and AI therapy insights",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd client && npm install"
  },
  "keywords": ["family", "communication", "therapy", "ai", "parenting"],
  "author": "Family Connect Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}